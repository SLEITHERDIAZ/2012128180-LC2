@model _2012128180_EN.Entities.LineaTelefonica

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>


@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>LineaTelefonica</h4>
        <hr />
        @Html.ValIdationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.AdministradorLineaId, "AdministradorLineaId", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("AdministradorLineaId", String.Empty)
                @Html.ValIdationMessageFor(model => model.AdministradorLineaId)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.VentasId, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.VentasId)
                @Html.ValIdationMessageFor(model => model.VentasId)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.EvaluacionId, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.EvaluacionId)
                @Html.ValIdationMessageFor(model => model.EvaluacionId)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

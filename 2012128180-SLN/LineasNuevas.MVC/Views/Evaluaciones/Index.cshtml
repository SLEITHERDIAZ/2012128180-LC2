@model IEnumerable<_2012128180_EN.Entities.Evaluacion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CentroAtencion.CentroAtencionId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Trabajador.NombreTra)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ventas.VentasId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VentasId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CentroAtencion.CentroAtencionId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Trabajador.NombreTra)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ventas.VentasId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VentasId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { Id=item.EvaluacionId }) |
            @Html.ActionLink("Details", "Details", new { Id=item.EvaluacionId }) |
            @Html.ActionLink("Delete", "Delete", new { Id=item.EvaluacionId })
        </td>
    </tr>
}

</table>

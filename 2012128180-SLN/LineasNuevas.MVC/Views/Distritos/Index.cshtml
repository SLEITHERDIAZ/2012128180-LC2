@model IEnumerable<_2012128180_EN.Entities.Distrito>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Provincia.NombrePro)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombreDis)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Provincia.NombrePro)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombreDis)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { Id=item.DistritoId }) |
            @Html.ActionLink("Details", "Details", new { Id=item.DistritoId }) |
            @Html.ActionLink("Delete", "Delete", new { Id=item.DistritoId })
        </td>
    </tr>
}

</table>

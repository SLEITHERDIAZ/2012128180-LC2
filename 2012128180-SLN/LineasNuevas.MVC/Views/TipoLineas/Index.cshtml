@model IEnumerable<_2012128180_EN.Entities.TipoLinea>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LineaTelefonicaId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LineaTelefonicaId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { Id=item.TipoLineaId }) |
            @Html.ActionLink("Details", "Details", new { Id=item.TipoLineaId }) |
            @Html.ActionLink("Delete", "Delete", new { Id=item.TipoLineaId })
        </td>
    </tr>
}

</table>

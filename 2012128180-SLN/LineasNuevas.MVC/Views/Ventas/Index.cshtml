@model IEnumerable<_2012128180_EN.Entities.Ventas>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CentroAtencion.CentroAtencionId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CentroAtencion.CentroAtencionId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { Id=item.VentasId }) |
            @Html.ActionLink("Details", "Details", new { Id=item.VentasId }) |
            @Html.ActionLink("Delete", "Delete", new { Id=item.VentasId })
        </td>
    </tr>
}

</table>
